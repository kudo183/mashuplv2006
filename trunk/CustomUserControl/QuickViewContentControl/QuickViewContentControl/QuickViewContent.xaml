<bl:BasicDataListItem x:Class="QuickViewContentControl.QuickViewContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bl="clr-namespace:BasicLibrary;assembly=BasicLibrary"
    mc:Ignorable="d"
    d:DesignHeight="285" d:DesignWidth="424">

    <Border x:Name="LayoutBorder" BorderBrush="Transparent" BorderThickness="0">
        <Grid x:Name="LayoutRoot" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Border x:Name="borderImage" BorderBrush="#EFEFEF" BorderThickness="1" Margin="2">
                <Image Grid.Row="0" x:Name="imgImage" Cursor="Hand" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Stretch="Uniform"></Image>
            </Border>
            <TextBlock Grid.Row="1" x:Name="tbTitle" Margin="6,1" Cursor="Hand" TextTrimming="WordEllipsis" FontFamily="Verdana" FontSize="18" Foreground="#0072BC" HorizontalAlignment="Left" VerticalAlignment="Center"></TextBlock>
            <TextBlock Grid.Row="2" x:Name="tbContent" Margin="6,0" TextWrapping="Wrap"></TextBlock>
        </Grid>
    </Border>
</bl:BasicDataListItem>
